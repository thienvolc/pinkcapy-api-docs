type: object
properties:
  old_password:
    type: string
    format: password
    description: The current password of the user.
  new_password:
    type: string
    format: password
    description: The new password to set for the user.
