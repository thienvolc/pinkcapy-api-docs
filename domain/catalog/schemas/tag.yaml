Tag:
  type: object
  properties:
    tag_id:
      type: integer
      readOnly: true
      example: 1
    title:
      type: string
      example: 'Phụ kiện điện tử'
    meta_title:
      type: string
      example: 'Phụ kiện điện tử chính hãng giá tốt'
    slug:
      type: string
      example: 'phu-kien-dien-tu'
    desc:
      type: string
      example: 'Tổng hợp các sản phẩm phụ kiện điện tử chất lượng cao.'
